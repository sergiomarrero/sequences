# Supabase (same project and tables as the crm app)
SUPABASE_URL=https://cwlsaxxfmhvmkgpbsflq.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# next-auth v5 (Google SSO). Generate AUTH_SECRET with: npx auth secret
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
# comma-separated allowlist; defaults to sergio@rbl1.com if unset
# ALLOWED_EMAILS=sergio@rbl1.com

# Bearer token for Claude's mailman run (scoped to /api/sequences only).
# Must match the SEQUENCES_SYNC_TOKEN in the Claude environment.
SEQUENCES_SYNC_TOKEN=
